# SmartContainer Risk Engine — Python Dependencies
# Install: pip install -r requirements.txt

# Core ML
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# REST API
flask>=3.0.0
flask-cors>=4.0.0

# Utilities
gunicorn>=21.0.0   # Production WSGI server (use instead of flask dev server)
reportlab>=4.0.0   # PDF generation for audit/governance report
